# Historical sketch. Live CI is `.github/workflows/ci.yml`.
#
# That workflow runs the Layer 1 gates from `docs/VERIFICATION.md`:
# fmt, clippy `-D warnings` on `boson-backend` + teaching host, contract
# tests, protected-boson-host check/run, and boson-backend rustdoc with
# broken-intra-doc-link deny. It does not build `boson-app` SSR/UI.
